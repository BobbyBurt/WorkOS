/** @format */

/** Alternate spellings and synonyms for keywords. Value correlates to keywords in the `keywordResults` map. */
export let keywordAlias = new Map<string, string>([
  ["algebra", "math"],
  ["game", "games"],
  ["ng", "newgrounds"],
]);
